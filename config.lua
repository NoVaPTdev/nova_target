Config = {
    -- Tecla para ativar o targeting (LMENU = ALT esquerdo)
    Key = 'LMENU',

    -- Distância máxima do raycast (em unidades do jogo)
    MaxDistance = 7.0,

    -- Distância máxima para desenhar sprites em zonas
    DrawDistance = 10.0,

    -- Registar exports compatíveis com ox_target e qb-target
    EnableCompat = true,

    -- Mostrar markers de debug para zonas
    Debug = false,
}
